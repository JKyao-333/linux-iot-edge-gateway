# Development configuration for local and WSL testing.

serial:
  devices:
    - /tmp/tty_gateway
  baud_rate: 115200
  reconnect_interval_seconds: 2

mqtt:
  host: localhost
  port: 1883
  topic_prefix: sensor
  cache_retry_interval_seconds: 5

tcp:
  enabled: true
  host: 127.0.0.1
  port: 9000

cache:
  type: sqlite
  path: data/pending_messages.db

log:
  path: logs/gateway.log
  level: DEBUG
