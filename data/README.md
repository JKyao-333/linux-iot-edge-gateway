# 运行数据目录

`data/` 用于保存网关运行期间产生的本地持久化数据，例如 MQTT 离线消息队列。

默认配置使用 SQLite 数据库 `pending_messages.db`。数据库、WAL/SHM 文件、旧版文件缓存、迁移备份和临时文件均属于本机运行状态，已由 `.gitignore` 排除，不应提交到仓库。

本目录只保留不含设备标识、凭据、网络地址和绝对路径的测试摘要或说明文件。
