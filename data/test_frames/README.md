# 脱敏测试帧

本目录保存由固定合成值生成的协议测试帧，不包含真实设备序列号、真实采集日志或现场数据。

- `valid_frames.hex`：CRC16-Modbus 校验正确的传感器帧。
- `fault_frames.hex`：CRC 错误、非法长度和前置噪声场景。
- `stream_cases.hex`：用于半包、连续帧和粘包回放。

这些帧只用于协议解析、故障注入和回归验证。新增样例前应确认其中没有设备标识、账户、绝对路径或现场采集数据。
